{% style %}
  .icon-facebook {
    background: white;
    border-radius: 50%;
    width: 20px;
    height: 20px; /* ← typo fix */
    color: black;
    display: inline-flex;         /* optional: icon center align */
    align-items: center;          /* optional */
    justify-content: center;      /* optional */
  }
{% endstyle %}

<i class="icon-facebook"></i>